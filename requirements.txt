# ResearchMind — requirements.txt
#
# Core data / ML stack -------------------------------------------------------
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12

# Semantic embeddings ---------------------------------------------------------
# Heavy, optional-at-runtime: if unavailable, ResearchMind automatically (and
# visibly) falls back to a TF-IDF + SVD embedding backend. Install these for
# the project's primary, spec-required all-mpnet-base-v2 embeddings.
sentence-transformers>=2.2
torch>=2.0

# Vector database ---------------------------------------------------------
# Optional at runtime: falls back to an exact numpy cosine-similarity search
# if unavailable.
faiss-cpu>=1.7

# NLP utilities ---------------------------------------------------------
nltk>=3.8

# Web app ---------------------------------------------------------
streamlit>=1.28

# Config / utils ---------------------------------------------------------
python-dotenv>=1.0
joblib>=1.3
requests>=2.31
tqdm>=4.66

# Testing ---------------------------------------------------------
pytest>=7.4

# LLM provider (only needed if LLM_PROVIDER=anthropic in .env) --------------
anthropic>=0.25
